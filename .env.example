# Copy to .env and fill in — l7_generation.py reads ANTHROPIC_API_KEY from the environment.
ANTHROPIC_API_KEY=

# Optional: only needed if you run KB-1 rate-limiting against a real Redis instance
# instead of the in-memory fallback (knowledge_bases/kb1_session_log.py).
REDIS_HOST=localhost
REDIS_PORT=6379
